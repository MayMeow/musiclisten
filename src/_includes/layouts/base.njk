<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ title or "Listening Log" }}</title>
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/assets/css/main.css" />
  </head>
  <body>
    <div class="container">
      {% set currentUrl = page.url or '/' %}
      {% set onHistory = currentUrl and currentUrl.startsWith('/history') %}
      {% set onTopAlbums = currentUrl and currentUrl.startsWith('/top-albums') %}
      <header class="site-nav">
        <a class="site-brand" href="/">music.maymeow.dev</a>
        <nav class="site-links">
          <a class="nav-link{% if not onHistory and not onTopAlbums %} active{% endif %}" href="/">
            Now Playing
          </a>
          <a class="nav-link{% if onTopAlbums %} active{% endif %}" href="/top-albums/">
            Top albums
          </a>
          <a class="nav-link{% if onHistory %} active{% endif %}" href="/history/">All History</a>
        </nav>
      </header>
      <main>{{ content | safe }}</main>
      <footer class="text-center small">
        Built with ❤️ using Eleventy, a custom Bootstrap build, and the Last.fm API.
      </footer>
    </div>
    <script src="/assets/js/bootstrap.bundle.min.js" defer></script>
  </body>
</html>
